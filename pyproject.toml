[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "netops-toolkit"
version = "2.0.0"
description = "网络工程实施及测试工具集 - 面向网络工程师的多功能CLI工具箱"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [
    {name = "Network Engineering Team", email = "netops@example.com"}
]
keywords = [
    "network", "automation", "cli", "ssh", "ping", "netmiko",
    "network-engineering", "diagnostics", "testing"
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: System Administrators",
    "Intended Audience :: Information Technology",
    "License :: OSI Approved :: MIT License",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: System :: Networking",
    "Topic :: System :: Systems Administration",
]
dependencies = [
    "rich>=14.3.0",
    "questionary>=2.0.1",
    "typer>=0.21.0",
    "textual>=7.4.0",
    "netmiko>=4.6.0",
    "ping3>=4.0.4",
    "dnspython>=2.8.0",
    "textfsm>=1.1.3",
    "pyyaml>=6.0.1",
    "cryptography>=46.0.0",
    "loguru>=0.7.2",
    "tabulate>=0.9.0",
    # Clean Architecture dependencies
    "pydantic>=2.10.0",
    "pydantic-settings>=2.7.0",
    "httpx>=0.28.0",
    "PyJWT>=2.8.0",
]

[project.optional-dependencies]
security = [
    "keyring>=25.0.0",
]
full = [
    "scapy>=2.6.0",
    "speedtest-cli>=2.1.3",
    "ntc-templates>=6.0.0",
    "pandas>=2.2.0",
    "openpyxl>=3.1.0",
    "keyring>=25.0.0",
    "napalm>=5.0.0",
    "ncclient>=0.6.15",
    "lxml>=5.0.0",
    "aiosmtplib>=3.0.0",
]
dev = [
    "pytest>=8.3.0",
    "pytest-cov>=6.0.0",
    "pytest-asyncio>=0.25.0",
    "black>=25.0.0",
    "mypy>=1.14.0",
    "ruff>=0.9.0",
    "textual-dev>=1.7.0",
]

[project.scripts]
netops = "netops_toolkit.cli:app"
netops-toolkit = "netops_toolkit.cli:app"

[project.urls]
Homepage = "https://github.com/netops-toolkit/netops-toolkit"
Documentation = "https://github.com/netops-toolkit/netops-toolkit#readme"
Repository = "https://github.com/netops-toolkit/netops-toolkit"
Issues = "https://github.com/netops-toolkit/netops-toolkit/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["netops_toolkit*"]

[tool.setuptools.package-data]
netops_toolkit = ["*.yaml", "templates/*.textfsm"]

[tool.black]
line-length = 100
target-version = ['py314']
include = '\.pyi?$'

[tool.ruff]
line-length = 100
select = ["E", "F", "W", "I", "N", "D", "UP"]
ignore = ["D100", "D104", "D107"]

[tool.mypy]
python_version = "3.14"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --cov=netops_toolkit --cov-report=term-missing"
asyncio_mode = "auto"
filterwarnings = [
    "ignore::DeprecationWarning:asyncio",
]

[tool.coverage.run]
source = ["netops_toolkit"]
omit = ["*/tests/*", "*/__pycache__/*"]
branch = true

[tool.coverage.report]
fail_under = 10  # incremental: 10→20→40→60 per sprint
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "if __name__ == .__main__.",
]
